server:
  port: 8080

spring:
  application:
    name: taskapp
  datasource:
    url: jdbc:postgresql://localhost:5432/taskdb
    username: taskuser
    password: taskpass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: my-secret-key-change-this-in-production-please-make-it-long
  expiration: 86400000
